{
    "name": "actus-root",
    "private": true,
    "scripts": {
        "bootstrap": "lerna bootstrap",
        "build": "lerna run build",
        "publish": "lerna publish",
        "test": "lerna run test"
    },
    "devDependencies": {
        "lerna": "^4.0.0"
    }
}
